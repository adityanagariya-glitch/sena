{
  "_comment": "Documents step is skipped in voice onboarding — app handles file uploads separately. Schema included for completeness and future reference.",
  "version": "v1",
  "step_id": "documents",
  "step_label": "Documents",
  "progress_percent": 80,
  "sections": [
    {
      "id": "documents_note",
      "label": "Documents",
      "fields": [
        {
          "id": "voice_skip_acknowledged",
          "type": "boolean",
          "label": "Participant acknowledged documents are uploaded via app",
          "required": false,
          "default": true
        }
      ]
    }
  ]
}
