# onboarding service — extra deps on top of services/requirements.base.txt
# LiveKit (onboarding uses the same shared voice pipeline)
livekit-api
livekit-protocol
grpcio
grpcio-status
proto-plus
protobuf
googleapis-common-protos

# Google Gemini — NEW unified SDK only. Provides `from google import genai`.
# Do NOT add google-generativeai (old SDK): it pins google-ai-generativelanguage
# which conflicts with google-genai and breaks the `google.genai` namespace.
google-genai
langchain