# casenote_monthly service — extra deps on top of services/requirements.base.txt
# Data analysis for trend stats and NDIS report generation
numpy
pandas
openpyxl

# Markdown → HTML rendering of generated reports (api_main.py: `import markdown`)
markdown
