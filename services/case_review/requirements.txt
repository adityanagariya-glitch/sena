# case_review service — extra deps on top of services/requirements.base.txt
# LangChain/LangGraph for RP detection pipeline
langchain-core
langchain-community
langchain-aws
langchain-openai
langchain-text-splitters
langgraph
mcp
openapi-pydantic
cachetools

# PDF/document processing (pymupdf for case note PDFs)
pymupdf
pillow
pillow-avif-plugin
pillow-heif
markitdown[all]
python-docx
openpyxl

# Google Gemini — NEW unified SDK only (shared voice engine: `from google import genai`).
# Do NOT add google-generativeai (old SDK): it pins google-ai-generativelanguage
# which conflicts with google-genai and breaks the `google.genai` namespace.
google-genai
langchain

# LiveKit — added per request for parity with voice/onboarding.
# NOTE: the import audit found NO `livekit` import in case_review's own code or
# in the shared modules it uses — so this is currently unused weight here.
livekit-api