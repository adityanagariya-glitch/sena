# OCR Service Project Configuration (services/ocr/pyproject.toml)
#
# Project: sena-ocr
# Description: Document OCR extraction service for Sena AI platform
#
# Dependencies:
# - sena-common: Shared library containing DB, middleware, and utilities
# - python-multipart: Handles multipart form data (file uploads)
#
# Purpose:
# - Defines the OCR service as a standalone Python package
# - Declares dependency on shared sena-common library
# - Uses setuptools for packaging and installation
