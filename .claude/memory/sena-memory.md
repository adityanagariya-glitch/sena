# Sena Session Memory

One line per significant task. Format:
`[YYYY-MM-DD] [agent name or "main"] — [what was done] — [key outcome]`

[2026-05-19] [main] — Applied SDAR paper (arXiv:2605.15155, "Self-Distilled Agentic Reinforcement Learning") as universal pattern for all 14 current agents + future agents + future AI features. We can't use SDAR's actual RL+OPSD training mechanics (Gemini/Bedrock are black-box APIs we don't fine-tune), but its asymmetric-trust principle ports directly to inference-time logic. Created `rules/asymmetric-privileged-trust.md` (canonical always-referenced — 3rd canonical alongside principal-engineer.md + sena-rules.md). All agents inherit via `principal-engineer.md` § Asymmetric Privileged Trust (new section). Three application layers: (1) agent reasoning — 14-row per-agent table mapping each agent to its valid positive-citation sources + required negative-rejection anchors; (2) feature implementation — teacher (privileged context) + student (vanilla) + gate, mapped onto Case Review / Onboarding / Voice / future features; (3) `/sena-learn` weighted-lessons upgrade (weighted_sum ≥ 3.0 vs raw count). Hard exemptions: tenant isolation, NDIS APP 8/11, deprecated Gemini, Pydantic v2, hook-enforced rules, user override — these always reject, no gating. Harness Phase 4 verifies section + file. REFERENCE.md §25 has regeneration template. Inventory bumped: canonical rules 2 → 3. Zero agent files edited — pure additive via canonical inheritance.

[2026-05-19] [main] — Added `/sena-learn` command — Claude's session self-improvement step. Scans this session for corrections (explicit / implicit / self-induced), de-dupes against `.claude/memory/lessons.md`, appends new entries with `Occurrences` counter, flags 3×-recurring patterns for promotion to CLAUDE.md (asks user before promoting). Includes anti-signal list (don't log "thanks" or topic shifts), repeated-mistake instant-fail check, and explicit distinction from `issues-solved/INDEX.md`. Wired into `sena-harness-upgrade` as **Phase 8 (LAST phase)** — every cycle ends with reflection. Detected and removed stray `sena-reflect.md` duplicate created by a prior unrelated process; merged its stronger content (self-induced mistake category, anti-signal list, behavioural-trigger phrasing, repeated-mistake instant-fail block, anti-patterns list) into `sena-learn.md`. Updated `sena-rules.md` inventory (20 → 21 commands; workflow count 5 → 6). Phase 5.5 in harness now expects 21 commands.

[2026-05-18] [main] — Added 13 missing direct-agent slash command shortcuts: `sena-planner`, `sena-task-breaker`, `sena-implementer`, `sena-business-reviewer`, `sena-security-reviewer`, `sena-bug-fixer`, `sena-optimization-reviewer`, `sena-cleaner`, `sena-git-committer`, `sena-log-analyzer`, `sena-researcher`, `sena-doc-writer`, `sena-code-reviewer`. Each is a thin shim with `allowed-tools: Agent` that routes `$ARGUMENTS` to its matching `subagent_type`. Commands now total 20 (5 workflow + 14 direct-agent + 1 generic `solve`). Updated `sena-rules.md` inventory (7 → 20). Updated harness Phase 5.5 to expect 20 commands. Added REFERENCE.md §16b (generic shim template) + §16c (per-agent purpose lines table with pipeline-position guidance) so missing shims can be regenerated.

[2026-05-15] [main] — Upgraded principal-engineer.md with 5 missing orchestration sections from user's skill (sub-agent delegation with spawn triggers + output-shape contracts + synthesis rules, explicit plan mode triggers, dynamic recalibration stop conditions, root-cause over symptom rules, elegance check 4-question pause, minimal blast radius → followups.md); added anti-patterns 11-13 (synthesize not paste, don't drift, no lesson repeats); added DoD followups.md item; added self-improvement enforcement (3× lesson → promote to CLAUDE.md). Wired into SESSION_START.md "Rules you MUST reload" and CLAUDE.md `<orchestration>` block. All 14 agents inherit automatically via canonical file reference.

[2026-05-15] [main] — Renamed `sena-cleanup-cycle` skill → `sena-harness-upgrade`. Directory + SKILL.md `name:` frontmatter + REFERENCE.md title all updated; SKILL.md description expanded to cover orchestration protocol wiring verification; new REFERENCE.md §4b documents 3 grep checks for principal-engineer.md / CLAUDE.md / SESSION_START.md alignment; Phase 4 + Phase 6 of the cycle now audit the 10 required `<orchestration>` sections.

[2026-05-15] [main] — `.claude/` harness upgrade pass: (1) deleted `client_onboarding_validations.md` (613 lines, wrong location), `gsd-instructions.md` (dead — no `/gsd-*` commands in commands/), `audits/xml-restructure-2026-04-22.md` + audits/ folder; (2) created `tasks/followups.md` (header-only, now wired) + `plans/README.md` (explains empty-by-design lifecycle); (3) split CLAUDE.md into autoload rules — moved gstack + Companion CLIs → `rules/external-tools.md` (load on demand via trigger_phrases frontmatter), Adding New Components protocol → `rules/add-component.md`; (4) trimmed CLAUDE.md from 1027 → 979 lines (auto-generated signatures section, ~375 lines, deferred — managed by `gen-context.js` script, requires reconfiguring the generator to write to `graphify-out/SIGNATURES.md` — logged in followups.md); (5) added 5 new slash commands: `/sena-harness-upgrade`, `/sena-plan`, `/sena-feature-ship`, `/sena-audit`, `/sena-status`.

[2026-05-15] [main] — Stale-ref sweep in `rules/`: fixed `principal-engineer.md` (`@agent-researcher` → `@agent-sena-researcher`), fixed `sena-rules.md` (removed broken `.agents/WORKFLOW_PLAN.md` ref, kept the tiered-policy summary inline; bumped routing-table audit date from 2026-05-08 to 2026-05-15; added explanation that `.agents/` was deleted 2026-05-11), added "Current Inventory" snapshot table to `sena-rules.md` (14 agents / 1 skill / 7 commands / 6 rules / 3 memory / 3 tasks files / plans-empty-by-design + hook flag state + just-deleted-file warning).

[2026-05-15] [main] — Wired this session's full pattern into `sena-harness-upgrade` skill so future runs replicate it: Phase 0 now flags non-standard root files (incl. patterns for the just-deleted set); Phase 2 has 5 grep pattern groups (deleted file refs, `.agents/` refs, pre-sena agent names, known-deleted files list, deprecated Gemini patterns); Phase 4 explicit grep for all 6 orchestration sub-headers + anti-patterns 11-13 + DoD followups.md line; NEW Phase 4.5 (autoload rules + supporting files — external-tools.md / add-component.md / tasks/followups.md / plans/README.md + CLAUDE.md ≤800 lines check); NEW Phase 5.5 (7-command slash inventory); Phase 7 also updates sena-rules.md "Current Inventory" + bumps routing audit date. REFERENCE.md gained §12-§16 templates (external-tools.md, add-component.md, followups.md, plans/README.md, 5 slash commands) so missing files can be regenerated from REFERENCE.md alone — no session transcript recovery needed.

[2026-05-15] [main] — Aggressive CLAUDE.md trim (1027 → 738 lines; human-edited content 652 → 364 lines, ~44% reduction). Created 7 new path-scoped autoload rules: `service-onboarding.md` (autoload on `services/onboarding/**`), `service-voice.md` (`services/voice/**`), `service-case-review.md` (`services/case_review/**`), `gemini.md` (`**/gemini*.py` + `**/demo_live*` — hook-gated, required Skill: gemini-live-api-dev invocation), `build-and-run.md` (`pyproject.toml` + `docker-compose*` + `Makefile` + `Dockerfile`), `deployment.md` (`Dockerfile` + `docker-compose.deploy.yml` + `.env.deploy*`), `demo-stack.md` (`demo_live*`). CLAUDE.md replaced full sections with brief pointers to these rules. Rules folder now has 13 files total (9 path-scoped + 2 trigger-phrase + 2 canonical).

[2026-05-15] [main] — Extended sena-harness-upgrade Phase 4.5 to enforce the new path-scoped rules split — verifies each of 7 new path-scoped rule files exists, verifies CLAUDE.md does NOT contain duplicated per-service deep-dives / Build & Run / Gemini rules / Demo Stack / Deployment content (should be pointers only). Added REFERENCE.md §17-§23 (one section per new rule file) with regeneration frontmatter + content outline. Phase 4.5 size check now targets human-edited section ≤400 lines (not just total ≤800) since auto-gen signatures (~375 lines) is managed by gen-context.js and out of scope. Bumped sena-rules.md Current Inventory rule count from 2 path-scoped → 9 path-scoped.

[2026-05-25] [main] — Installed and wired code-review-graph (tirth8205/code-review-graph v2.3.3, Tree-sitter AST → SQLite). Built graph: 165 files, 784 nodes, 5450 edges, 56 flows, 10 communities (branch voice-assistance-optimization). MCP server wired in `.claude/settings.json` using `C:\Users\Admin\Downloads\SENA\.venv\Scripts\code-review-graph.exe` (SENA_AI venv blocked by locked uvicorn.exe from running service). Added blast-radius rules to: CLAUDE.md (new `## code-review-graph` section), SESSION_START.md (READ-IF-RELEVANT row + "Rules you MUST reload" bullet), principal-engineer.md (step 0 in SENA pre-code checklist). Rule: before editing any file with 3+ importers, run `get_dependents` / `get_callers` / `get_affected_tests`; if tests > 5 or callers span services → flag user first.

## Log

2026-05-08 main — Bootstrapped Sena Agent System (created `.claude/memory/`, `.claude/rules/`, three memory files, rules file; appended Sena Agent System section to CLAUDE.md) — Workflow + routing infrastructure now persistent across sessions; SessionStart hook already wired in `settings.json` so it was left untouched.

2026-05-08 main — Migrated 8 SENA pipeline agents from `.agents/01-08*.md` (XML templates, wrong format) to `.claude/agents/sena-*.md` (proper Claude Code subagents with YAML frontmatter, model + tools locks, strong PROACTIVELY/MUST BE USED routing language) — Now invokable via `@agent-sena-*` and the Task tool. Added `sena-bug-fixer` between flag-only reviewers (business, security) and re-review; updated those reviewers to hand off to bug-fixer instead of looping back to implementer.

2026-05-11 main — Removed redundant `.agents/01-08*.md` + `.agents/README.md` + `.agents/WORKFLOW_PLAN.md` (superseded by `.claude/agents/sena-*.md`) — `.agents/` directory now contains only the pre-existing `skills/` subfolder; no leftover XML templates to confuse future sessions about which file is the source of truth for an agent's behaviour.

2026-05-11 main — `.claude/` gap-fill audit per Claude Code 2025 reference; ADDITIVE ONLY (no overwrites). Added: `CLAUDE.local.md` (gitignored personal overrides), 4 non-overlapping generic agents (researcher, log-analyzer, doc-writer, code-reviewer), 2 path-scoped rules (api.md, database.md), `PreCompact.sh` hook for snapshot-on-compaction, 2 skills (commit-push-pr, review-pr), `permissions.deny` block in `settings.json` (rm-rf, force-push, secret-reads), and a compact `## Hard Limits` section in `CLAUDE.md`. SKIPPED: generic `debugger / test-writer / refactorer / security-auditor` agents (would dilute SENA-specific routing — SENA already has stronger personas for each), `rules/frontend.md` (Flutter is a separate parent repo), `lint-on-save.sh` (existing `post-tool-use.sh` chain already covers Python edits via pipreqs+bump). Decision log appended.

2026-05-11 main — Closed the 3 remaining gaps vs the canonical `.claude/` reference image: added `.claude/output-styles/terse.md` (code-only response mode with SENA shorthand: FD/OS/FH/SR/FS; triggers via `/output-style terse`), `.claude/plugins/README.md` (empty-by-design placeholder explaining SENA's agent pipeline lives in `.claude/agents/` not as a packaged plugin; lists which external plugins are already installed globally), `.claude/statusline` (bash script printing `[SENA] <branch>* <service> [<n> tasks] [⚠ <flag-warning>]`; wired into `settings.json` via `statusLine.command`). The statusline auto-detects active service from cwd and warns when a hook flag is missing.

[2026-05-12] [orchestrator] — 6-agent voice/typed validation parity (Step 1) — shipped flutterhandoffdev.md + backend POST /errors + FieldValue.input_method + per-write cross-field check + reconciled emergency-contact strings (frontend impl pending)

[2026-05-12] [agent-05-qa] — QA pass on the 6-agent voice/typed parity ship — promoted paired on-screen/voice AppStrings keys for 12 spoken-divergent strings (voiceDobRequired, voiceDateInvalidFormat, voiceDateMustNotBeFuture, voiceDateMustBeAtLeast18Years, voiceAuStateInvalid, voiceAustralianMobileInvalid, voiceEmailInvalid, voiceGenderRequired, voicePreferredLanguagesRequired, voiceStateRequired, voiceAboutMeRequired, voiceFullNameFirstTooLong, voiceFullNameLastTooLong); added explicit 5-invariant block to handoff section 1 (mirrors SESSION_START + 0006 file byte-for-byte); reconciled handoff size claim across TASKS.md / 0006 / handoff itself (now 1651 lines, 91.8 KB); flagged INDEX.md numbering — 0004 file never written, slot reserved, next free is 0007 — backend ↔ handoff section 12 cross-check came back green (routes.py model strict, FieldValue.input_method optional Literal, build_envelope surfaces input_method, _update_field threads voice + runs per-write cross-field for emergency contacts, _v_boolean_required + _v_text250_required wired on basics.interpreter_required + basics.about_me, four byte-exact reason_human strings match section 12.5, append_client_validation_error uses sena:onboarding:errors:{sid} with 7-day TTL).

2026-05-14 main — Post-deployment cleanup: deleted 8 stale/dev files (AGENTS.md, FLUTTER_DEV_HANDOFF.md, FLUTTER_CROSS_SCREEN_CONTEXT_CONTRACT.md, no-graceful-muffin.md, v3-v4-enum-routines.md, test_harness.html, AI_logs.txt, nul); updated CLAUDE.md/SESSION_START.md/CLAUDE.local.md to remove dead references; created CODE_FILES_TO_REVIEW.md listing harness routes in main.py + demo stack for user decision — voice assistance now deployed on EC2.

═══════════════════════════════════════════════════════════════════════════════
  MILESTONE — 2026-05-14 — VOICE ASSISTANCE FEATURE CLOSED + CASE REVIEW SHELVED
═══════════════════════════════════════════════════════════════════════════════

2026-05-14 main — Clean-slate reset of `.claude/` for next feature. Archived all 12 completed/shelved task entries (#1, #2, #9-#17 voice + #10 case-review) into `.claude/tasks/ARCHIVE.md`. Rewrote `TASKS.md` with empty Active section + backlog of demo/dev decisions and infrastructure-blocked items. Rewrote `SESSION_START.md` — removed all per-feature ✅ DONE blocks, kept architecture pointers + service-run commands + Gemini Live rules. Updated `CLAUDE.local.md` to clear "Currently iterating on" + task pointers. Demo stack files (`demo_live_server.py`, `demo_client.html`, harness routes in `main.py`) retained per user decision but no longer referenced from SESSION_START.md. Voice + case-review context will NOT bleed into the next feature's planning. Preserved durable artefacts: `rules/api.md` (WS event canon), `rules/database.md` (Redis key canon), `rules/sena-rules.md` (agent pipeline), `memory/decisions.md` (decisions log), `output-styles/terse.md`, `issues-solved/`, `flutterhandoffdev.md` (Flutter still owes Step-1 implementation), `client_onboarding_validations.md`. Next session starts on a fresh `Active` section in `TASKS.md`.

2026-05-14 main — Installed 4 of 5 user-global skill packs + CLIs at `~/.claude/`: gstack (cloned + ./setup ran; 47 skills + native binaries; user-global config at `~/.gstack/config.yaml`), `agent-browser@0.27.0` (npm-global, Chromium-only — Firefox not supported), `specify@0.8.10.dev0` (uv tool global), `@deeplake/hivemind@0.7.24` (npm-global package — `hivemind install` wireup HELD pending APP 8/11 data residency call), `bun@1.3.14` (gstack prereq). dotclaude delegated to user (slash-command install). Added `## gstack` + `## Companion CLIs` sections to SENA CLAUDE.md with routing matrix: SENA agents take precedence for SENA-touching code (`/review-pr`, `@agent-sena-*` pipeline), gstack used for browser QA (`/qa`, `/browse`), design (`/design-*`), root-cause investigation (`/investigate`), and product-level checks (`/office-hours`). Explicit DO-NOT list: don't use gstack `/review` or `/ship` for SENA paths (would bypass tenant-isolation + NDIS audits); don't use `mcp__claude-in-chrome__*` MCP tools (use `agent-browser` or `/browse` instead).

2026-05-14 main — Flattened agent namespace to `sena-*` prefix + added Agent Routing Mandate + Workflow Orchestration to CLAUDE.md. Renamed 5 generic agents (code-reviewer, doc-writer, log-analyzer, researcher, disciplined-engineering-collaborator) to `sena-code-reviewer`, `sena-doc-writer`, `sena-log-analyzer`, `sena-researcher`, `sena-engineering-collaborator`. Updated all references: agent name: frontmatter, internal `For <agent>:` labels in principal_engineer_mode blocks, sena-rules.md routing table (added 4 new rows for the previously-unlisted generic agents), review-pr skill, CLAUDE.local.md shortcuts, sena-log-analyzer cross-ref to engineering-collaborator, renamed command file `commands/disciplined-engineering-collaborator.md` → `commands/sena-engineering-collaborator.md`. Added 2 new mandatory blocks to CLAUDE.md above "Principal Engineer Operating Mode": (1) `<agent_routing enforcement="check-before-reply">` with 13-row trigger table that fixes "Claude not auto-routing to agents" — main thread must consult the table before replying; (2) `<orchestration>` block consolidating user-supplied orchestration_protocols + Workflow Orchestration prompts (plan-first default, sub-agent delegation, verification gate, autonomous execution, self-improvement loop). New file `.claude/memory/lessons.md` seeded with format header — captures user-correction failure patterns (distinct from `issues-solved/INDEX.md` which captures technical bug recipes).

2026-05-14 main — Wired Principal Engineer Operating Mode into the agent system. Wrote canonical rules file at `.claude/rules/principal-engineer.md` (full prompt — 5 non-negotiable rules, recon/execute/report workflow, anti-pattern list, definition-of-done checklist, SENA-specific pre-code Grep checklist). Injected a `<principal_engineer_mode>` block into all 14 agents (`.claude/agents/*.md`) right after each agent's `<role>`/`<identity>` opening — block contains the 5 pinned rules + a one-line agent-specific mapping (e.g. sena-implementer: "Grep services/ + shared/ before adding a helper"; sena-business-reviewer: "reinvented existing helper is now a STATUS: FAIL"; sena-planner: "DAG must include an 'Existing code to extend' section before any 'New file to create' subtask"; sena-cleaner: "BLOCK on duplicated stdlib/library helpers"; sena-security-reviewer: "custom crypto/JWT/Redis-key-builder is INSTANT Critical"). Added a compact summary section to `CLAUDE.md` above "Hard Limits" pointing to the canonical file. Now every agent + the main session operates under the same anti-bloat / anti-reinvention / minimal-diff doctrine; future rule changes happen in one file and propagate.

2026-05-11 main — Deep SENA customisation pass on the additive agents/rules/skills from earlier today. Embedded SENA-specific knowledge directly into prompts so they no longer rely on generic priors: (1) researcher now has an authoritative-sources table (Gemini Live llms.txt, Pydantic v2 only, NDIS docs); (2) log-analyzer has a SENA crash-signature lookup table (deprecated Gemini patterns, missing tenant_id, cross_section_blocked, etc.) that routes findings to the right downstream agent; (3) doc-writer has a SENA doc inventory (CLAUDE.md, FLUTTER_DEV_HANDOFF.md, onboarding_system.md, TASKS.md, issues-solved/) with tone conventions per file; (4) code-reviewer has an `auto_block_signatures` table with 13 SENA forbidden patterns (deprecated Gemini API, missing tenant_id, Pydantic v1, _agent_speaking flag, etc.) that force BLOCK even on non-SENA paths in this monorepo; (5) rules/api.md gained the full SENA WebSocket event canonical list (Flutter contract); (6) rules/database.md gained the full SENA Redis key inventory; (7) PreCompact.sh now captures branch+commit+dirty-state+active-task-count; (8) CLAUDE.local.md gained paste-ready SENA commands and current task pointers; (9) commit-push-pr now detects only-touched-services for testing, has a forbidden-content scan, and a Conventional Commits scope cheat sheet; (10) review-pr expanded the routing table from 4 rows to 16 to cover every SENA path category, including new-tool + new-WS-event + new-Redis-key as triggers for combined reviewer runs.
