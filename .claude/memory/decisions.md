# Sena Decisions Log

Format: `[YYYY-MM-DD] — [Decision] — [Why]`

2026-05-15 — Wired this session's `.claude/` upgrade pattern into `sena-harness-upgrade` skill — Future runs of `/sena-harness-upgrade` (or `/sena-audit`) will replicate today's work automatically. Added: Phase 0 check for non-standard root files (auto-flag `client_onboarding_validations.md`, `gsd-instructions.md`, `audits/`-style folders); Phase 2 comprehensive stale-ref patterns (`.agents/` refs, pre-`sena-` agent names, deleted file refs, deprecated Gemini patterns); Phase 4 explicit grep for 6 orchestration sub-headers (Sub-Agent Delegation, Plan Mode Triggers, Dynamic Recalibration, Root Cause Over Symptom, Elegance Check, Minimal Blast Radius) + anti-patterns 11-13 + DoD followups.md line; NEW Phase 4.5 (autoload rules + supporting files audit — verifies `rules/external-tools.md`, `rules/add-component.md`, `tasks/followups.md`, `plans/README.md` exist, that CLAUDE.md uses pointers not duplicates, that CLAUDE.md is under 800 lines); NEW Phase 5.5 (slash commands inventory — verifies 7 SENA commands present); Phase 7 also updates sena-rules.md "Current Inventory" table and bumps the routing-table audit date. REFERENCE.md gained §12-§16 templates for external-tools.md, add-component.md, followups.md, plans/README.md, and 5 slash commands so missing files can be regenerated without recovering from this session's transcript.

2026-05-15 — `.claude/rules/sena-rules.md` gained a "Current Inventory" snapshot table — Was: routing table only; agents/skills/commands/rules count was implicit and stale. Now: explicit table at the bottom of the file with paths and counts; single source of truth for "what's in `.claude/`." Bumps every harness-upgrade run (Phase 7).

2026-05-15 — `principal-engineer.md` is now the canonical single source for the Principal Engineer rules INCLUDING orchestration protocols — Was: 5 non-negotiable rules only. Added: 6 orchestration sections (Sub-Agent Delegation, Plan Mode Triggers, Dynamic Recalibration, Root Cause Over Symptom, Elegance Check, Minimal Blast Radius), 3 new anti-patterns (synthesize-not-paste, no-drift, no-lesson-repeat), DoD followups.md line, self-improvement enforcement (3× lesson → CLAUDE.md). All 14 agents auto-inherit via the `<principal_engineer_mode>` reference block. CLAUDE.md `<orchestration>` block carries the same content for main-session enforcement (intentional duplication — future drift detection lives in the harness skill's Phase 6).

2026-05-15 — Split CLAUDE.md content into trigger-phrase autoload rules — Was: gstack section (~32 lines) + Companion CLIs (~5 lines) + Adding-New-Components protocol (~21 lines) loaded every session regardless of relevance. Now: `rules/external-tools.md` (autoload via gstack/browse/design/investigate/specify/hivemind triggers) and `rules/add-component.md` (autoload via "I am adding X" triggers). CLAUDE.md retains 1-line pointers. Tokens saved per session: ~58 lines from CLAUDE.md, no functional loss because the rules load on-demand when needed.

2026-05-15 — Sena-cleanup-cycle skill renamed → sena-harness-upgrade — Was: name implied "cleanup only," but the skill now also UPGRADES the harness (principal-engineer rules, autoload-rule structure, slash commands). New name covers both functions. Directory renamed; SKILL.md `name:` frontmatter + REFERENCE.md title updated; all SKILL.md references updated. Old name should not appear in any live file (Phase 2 sweep would catch).

2026-05-15 — Aggressive CLAUDE.md split into 7 new path-scoped rules + brief pointers (1027 → 738 lines, ~44% reduction in human-edited content) — Was: every session loaded full per-service deep-dives (~145 lines), full Build & Run command listing (~60 lines), full Gemini API rules block (~35 lines), Demo Stack (~10 lines), Deployment EC2 (~25 lines) regardless of relevance. Now: `rules/service-onboarding.md` autoloads on `services/onboarding/**`, `rules/service-voice.md` on `services/voice/**`, `rules/service-case-review.md` on `services/case_review/**`, `rules/gemini.md` on `**/gemini*.py` + `**/demo_live*` (hook-gated), `rules/build-and-run.md` on `pyproject.toml` + `docker-compose*.yml` + `Makefile` + `Dockerfile`, `rules/deployment.md` on `Dockerfile` + `docker-compose.deploy.yml` + `.env.deploy*`, `rules/demo-stack.md` on `demo_live*`. CLAUDE.md retains LLM split table + autoload-pointer table. Token savings per session: ~275 lines of always-loaded content moved to load-when-needed. Harness Phase 4.5 enforces this split — verifies the 9 path-scoped rules exist AND that CLAUDE.md doesn't contain duplicated content. REFERENCE.md §17-§23 has templates for regeneration if any rule file is deleted.

2026-05-15 — Rules now classified by autoload behavior: 9 path-scoped (auto on file edit match) + 2 trigger-phrase (auto on keyword match) + 2 canonical always-referenced — Was: rules folder mixed always-loaded and on-demand without clear taxonomy. Now: explicit classification in `sena-rules.md` Current Inventory table. Trigger-phrase rules use `description:` + `trigger_phrases:` frontmatter; path-scoped use `paths:` glob frontmatter; canonical have neither (loaded via `<principal_engineer_mode>` reference in agents and via session-start read).

2026-05-08 — `.agents/` (numbered pipeline templates) kept separate from `.claude/agents/` (Claude Code subagents) — Different runtimes: `.claude/agents/` files are loaded by the Task tool; `.agents/01-08*.md` are XML prompt templates for an external multi-model orchestrator (Opus plan → Sonnet exec → Haiku gate, per `.agents/WORKFLOW_PLAN.md`). Mixing them would cause Claude Code to attempt-load files without frontmatter and fail.

2026-05-08 — Default retry cap kept at "max 2" in `sena-rules.md` despite `WORKFLOW_PLAN.md` proposing a tiered policy (A unlimited / B ≤2 / C zero) — The tiered policy depends on the orchestrator being wired; for current in-session subagent work, the flat default is simpler and the rules file points to the richer policy as a forward reference.

2026-05-08 — SessionStart hook NOT replaced — The existing `.claude/hooks/session-start.sh` is integrated with the broader hook system (graphify rebuild, SESSION_START.md injection, ctx7/skills flag gating) and is referenced by other hooks. Replacing it with the script's minimal cat-rules-and-memory version would break that integration.

2026-05-11 — Generic `debugger / test-writer / refactorer / security-auditor` agents NOT added — Each would overlap with a stronger SENA-specific persona (sena-bug-fixer, sena-implementer, sena-optimization-reviewer, sena-security-reviewer respectively). Claude Code picks agents by description-match score; weaker generic descriptions would lose to SENA's "MUST BE USED" routing language, so adding them produces clutter without changing behaviour. Only added agents that fill genuine gaps: researcher (web), log-analyzer (crash parse), doc-writer (FLUTTER_DEV_HANDOFF style), code-reviewer (ad-hoc external diffs only — explicitly forbidden from reviewing SENA paths).

2026-05-11 — `rules/frontend.md` NOT added — The SENA AI repo is Python backend microservices only. The Flutter frontend lives in the parent `sena-mobile/` repo (separate project, separate `.claude/`). A `rules/frontend.md` here would either be empty or describe a codebase that does not exist in this tree.

2026-05-11 — `hooks/lint-on-save.sh` NOT added — The existing `post-tool-use.sh` already invokes pipreqs on Python file edits and the chain runs `bump-updated.sh` for frontmatter timestamps. Adding a second lint hook on Write|Edit would either double-run ruff or race with `post-tool-use.sh`. If Python lint-on-save is wanted explicitly, the cleaner pattern is to extend `post-tool-use.sh` rather than add a parallel hook.

2026-05-11 — `permissions.deny` added to project `settings.json` (not `settings.local.json`) — Deny rules are team-wide safety guardrails (rm -rf, force-push, secret-file reads). Putting them in shared `settings.json` means every dev on the team gets them. The existing `settings.local.json` has the personal allow-list; that split (deny=shared, allow=personal) follows the user's guidance.

2026-05-14 — Onboarding voice assistance deployed to EC2 (Docker, `docker-compose.deploy.yml`) — Service is live; Redis-only state, nginx reverse proxy expected in front. `sena-ai/services/onboarding/` port 8083 is the canonical production service. Demo stack (`demo_live_server.py` / `demo_client.html`) is dev-only and pending removal decision.

2026-05-14 — `FLUTTER_DEV_HANDOFF.md` deleted; `flutterhandoffdev.md` is now the sole canonical Flutter contract — The old handoff was an append-only issues log (#1–#28) that accumulated contradictions. The new file was written clean by the 6-agent orchestration (2026-05-12) as a structured Step-1 validation contract. All future Flutter WS event additions go to `flutterhandoffdev.md`.

2026-05-14 — `AGENTS.md` deleted — Codex-style guidance file; all content duplicated in `CLAUDE.md` + `.claude/rules/sena-rules.md`. Referenced stale "0/9 phases" state (voice is deployed). No unique content worth preserving.

2026-05-14 — Completed plan files (`no-graceful-muffin.md`, `v3-v4-enum-routines.md`) deleted from `.claude/plans/` — Plans for Tasks #12 and #16/#17 are fully executed. Git history is the archive. `.claude/plans/` should only hold in-flight plans.

2026-05-14 — Voice assistance + case-review task histories archived into `.claude/tasks/ARCHIVE.md`; `TASKS.md` reset to empty `Active` — User directive: "voice was one feature, next we'll work on something else, context should not merge". Mixing 12 completed task entries into the live queue was leaking voice-specific assumptions into next-feature planning (e.g. Flutter Issues #18-#28, JSON-as-Truth Protocol, cross-screen bucket flag) that may have no bearing on whatever ships next. Keeping ARCHIVE.md preserves the full history for any future investigation while making the live `TASKS.md` start clean.

2026-05-14 — Case Review service (Phase A-C shipped, D-G unstarted) **shelved**, not just paused — User chose "Archive entirely" over "Move to backlog". Phase D-G are not on the active roadmap; re-opening requires user direction. `.planning/CASE_NOTE_REVIEW_PLAN.md` remains in the repo as the resume-point if work picks up again. Service code at `services/case_review/` is untouched; running it still works.

2026-05-14 — Demo stack (`demo_live_server.py`, `demo_client.html`, harness routes in `main.py`) retained in repo but de-referenced from `SESSION_START.md` — User chose "Keep, hide from session start". Files remain for Gemini Live regression debugging if needed, but new sessions no longer surface them as active services. `CLAUDE.md` still mentions the demo stack in its dedicated section (informational; not load-bearing for routing).

2026-05-14 — User-global skill pack + CLIs installed at `~/.claude/` (gstack + agent-browser + specify + hivemind + bun) — User wanted these 5 external repos available in every project (`https://github.com/garrytan/gstack`, `https://github.com/vercel-labs/agent-browser`, `https://github.com/github/spec-kit`, `https://github.com/activeloopai/hivemind`, `https://github.com/poshan0126/dotclaude`). Installed: (1) gstack cloned to `~/.claude/skills/gstack`, `./setup --no-prefix --yes` ran (47 skills + native binaries `browse.exe`, `design.exe`, `pdf.exe`, `server-node.mjs`); global config at `~/.gstack/config.yaml`; (2) `agent-browser@0.27.0` npm-global — Chromium-only, user asked for Firefox but agent-browser does NOT support Firefox (only Chrome/Brave/Edge/iOS Safari/cloud providers); `agent-browser install` (Chrome download) NOT run pending user pick; (3) `specify@0.8.10.dev0` via `uv tool install` — `~/.local/bin` added to PATH; (4) `@deeplake/hivemind@0.7.24` npm-global package installed, **`hivemind install` wireup HELD** pending Australian data residency decision (Deeplake captures every session prompt+tool-call+response and ships to cloud — APP 8 + APP 11 + NDIS data residency risk for SENA work). (5) `bun@1.3.14` npm-global (gstack prerequisite). dotclaude install delegated to user (slash commands `/plugin marketplace add poshan0126/dotclaude` + `/plugin install setupdotclaude@dotclaude` — must NOT run `/setupdotclaude` inside SENA project, would clobber `.claude/`). Added `## gstack` + `## Companion CLIs` sections to SENA `CLAUDE.md` so SENA sessions know gstack is available but should NOT use gstack `/review` or `/ship` for SENA-touching diffs (canonical SENA pipeline takes precedence).

2026-05-14 — Agent namespace flattened to `sena-*` prefix for ALL 14 agents — Renamed `code-reviewer` → `sena-code-reviewer`, `doc-writer` → `sena-doc-writer`, `log-analyzer` → `sena-log-analyzer`, `researcher` → `sena-researcher`, `disciplined-engineering-collaborator` → `sena-engineering-collaborator` (also renamed `.claude/commands/disciplined-engineering-collaborator.md` to match). Rationale: user reported inconsistent naming made the routing table noisy. All references updated in: agent files' `name:` frontmatter, agent files' internal `**For <agent>:**` labels, `.claude/rules/sena-rules.md` routing table (also added 4 new rows for the previously-unlisted generic agents), `.claude/skills/review-pr/SKILL.md`, `CLAUDE.local.md` shortcuts, `sena-log-analyzer.md` cross-reference to engineering-collaborator. Historical references in `memory/sena-memory.md` log entries left untouched (they accurately describe past names). The `name:` field is what Claude Code routes against, so the rename is functional, not cosmetic.

2026-05-14 — `Agent Routing Mandate` + `Workflow Orchestration` sections added to `CLAUDE.md` — User reported "Claude is not automatically using the relevant agent — I have to tell it explicitly." Fix: added a top-level `<agent_routing priority="MANDATORY" enforcement="check-before-reply">` block in CLAUDE.md with a 13-row trigger table that maps user-request shapes to the right `@agent-sena-*` agent. Above the Principal Engineer block, below the existing protocols. Also added `<orchestration>` block (sub-agent delegation discipline, plan-first default, verification gate, autonomous execution, self-improvement loop). New file `.claude/memory/lessons.md` (failure-pattern log distinct from `issues-solved/INDEX.md`) seeded for the self-improvement loop. The orchestration content was consolidated from two user-supplied prompts (XML `<orchestration_protocols>` block + markdown `## Workflow Orchestration` section) — same content in two formats.

2026-05-14 — Principal Engineer Operating Mode wired into all 14 agents + `CLAUDE.md` — Canonical rules live at `.claude/rules/principal-engineer.md`. Every agent in `.claude/agents/` got a `<principal_engineer_mode>` block injected after its `<role>`/`<identity>` opening, with a uniform 5-rule pin (no reinvention / no bloat / no stubs / stay in scope / optimization is default) plus a one-line agent-specific mapping. CLAUDE.md got a compact summary section above "Hard Limits" pointing to the canonical file. Rationale: the user's "Elite Principal Engineer" prompt was too long to drop verbatim into each agent file; centralising the rules + injecting a compact pinned reminder per agent gives every workflow a deterministic anti-bloat / anti-reinvention gate without duplicating prose. Future rule changes happen in ONE place (`principal-engineer.md`); agents pick them up automatically because they pin "operate under the rules in <path>".
