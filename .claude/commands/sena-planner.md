---
description: Direct invocation of @agent-sena-planner — architectural plan + subtask DAG + NDIS compliance + tenant-isolation analysis. Does NOT write code.
allowed-tools: Agent
---

Route the user's requirement to `sena-planner` (Agent tool, subagent_type: sena-planner). Returns: Existing code to extend / Libraries to use → New files to create → file-by-file plan → subtask DAG → NDIS/tenant implications → rollback. **Does not write code.**

User's args: $ARGUMENTS
