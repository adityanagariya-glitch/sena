---
title: Follow-ups — Out-of-Scope Observations
updated: 2026-05-15
purpose: Catch-all for issues discovered while working on something else. Drop one-line observations here; the principal-engineer "minimal blast radius" rule routes them here instead of polluting the current diff. Triage on next planner run.
---

# Follow-ups

Format: `[YYYY-MM-DD] [discovered-during] — [observation] — [next action]`

## Open

- [2026-05-15] [during harness-upgrade] — CLAUDE.md auto-generated signatures section (~375 lines) is regenerated by `gen-context.js` (marker at line 605: `<!-- Updated by gen-context.js -->`). Manual removal will be reverted on next run. — **Next action:** locate `gen-context.js` (likely repo root or `tools/`), reconfigure it to write to `graphify-out/SIGNATURES.md` instead of CLAUDE.md, OR delete the marker if signatures aren't actively used. Decision needed.
- [2026-06-03] [during .github cleanup] — `.github/` folder (`copilot-instructions.md` + `context-cold.md`) deleted — user not using GitHub Copilot; Claude never loaded either (Copilot's hot context + SigMap's MCP-only cold context). BOTH FILES ARE ALSO REGENERATED BY `gen-context.js` (same script as the CLAUDE.md signatures issue above). Will reappear on next gen-context.js run. — **Next action:** when reconfiguring gen-context.js for the CLAUDE.md issue, ALSO disable its `.github/` output paths (likely a config flag like `outputs.copilot` and `outputs.cold` in the script's config block). Single fix kills three regenerations.
- [2026-05-18] [during voice per-screen-state harden patch] — `screen_field_status` kwarg is plumbed through `sequencing.next_required_field` + `prompt_builder._compute_next_required_field` + `_build_live_state_block` + `build_system_prompt`, but `gemini_live.py` does NOT yet pass the latest `screen_state_v2.field_status` to `build_system_prompt`. Until wired, the param is dormant. — **Next action:** locate the prompt rebuild path in `services/gemini_live.py`, store the last-seen `field_status` map on the session object, pass it on every `build_system_prompt` call. ~10-line change.
- [2026-05-18] [during voice per-screen-state harden patch] — `screen_field_status` honors SCALAR field paths only (e.g. `basics.address`). REPEATABLE sections (e.g. `emergency_contacts[0].email`) walk `_section_min_unmet` first, which doesn't consult `screen_field_status`. Symptom: user reports voice agent "remembers" filled scalars when reminded but NOT emergency contacts. — **Next action:** extend the walker in BOTH `sequencing.next_required_field` and `prompt_builder._compute_next_required_field` to accept row-indexed paths (e.g. `emergency_contacts[0].email: filled`) AND propagate that through `_section_min_unmet` so a filled row counts toward the min-rows gate even when FormState hasn't seeded it yet.
- [2026-05-18] [during email validator spec alignment] — `cross_field.check_emergency_phone_unique_and_differs_from_client` does NOT normalize AU phone formats before duplicate-compare. `0412345678` and `+61412345678` are the same number but treated as different → false-negative on duplicate detection. Test `test_emergency_phone_duplicate_reason_human_bytematch` is failing pre-existing (not introduced this session). — **Next action:** add a `_normalize_au_phone()` helper (strip whitespace, convert `04XX...` → `+614XX...`) and call it before set/list comparison in both `check_emergency_phone_*` functions. Add positive + negative tests.
- [2026-05-25] [during Option D per-screen-session bug-fixer pass] — Rollback asymmetry: when `onboarding_tool_state_channel=False`, the prompt §1/§1a/§1b retain Option-D-shaped wording (tool-reply source-of-truth, `get_current_state` staleness check) which is misleading in full-bootstrap mode. Intentional per spec §7.11 (flag is emergency rollback only). — **Next action:** if the flag is ever used in production rollback, update §1/§1a/§1b to restore legacy phrasing as a separate prompt variant.
- [2026-06-02] [during per-screen/per-minute usage rollup wiring] — `gemini_live.py:867,869` carry two pre-existing `E501` ruff violations (104 and 109 chars) in the usage-delta math block (`d_response = max(...)` and the `if d_prompt or ... self._tool_calls_in_turn:` guard). NOT introduced by the step_id edit; left untouched to keep that diff focused + avoid churn in Gemini-gated code. — **Next action:** wrap both lines (split the `max(...)` args; parenthesise the `if` condition across lines). ~4-line lint-only change; no functional impact. Safe for any cleaner pass.
- [2026-06-02] [usage consolidation] — User decision: MongoDB (`usage_logs`, one doc per user with `screens[]`) is the ONLY token-usage sink. DELETED same day: `cost_calculator.py`, `fx_rate.py`, `usage_cost_summary.py`, `usage_events_sink.py`, `usage_event_repo.py`, migration `0002_create_usage_event.py`, + their 3 test files (all were uncommitted — gone permanently). `emit_usage` remains the chokepoint but now only forwards to Mongo. — **Next action:** none; recorded so nobody hunts for the "missing" cost/AUD layer. If $-cost estimation is ever needed again, it must be rebuilt (rates were: Gemini 3 Flash $0.50/$3.00, 3.1 Live text $0.75/$4.50 + audio $3/$12, Bedrock Claude 3.5 $6/$30 per 1M, as of 2026-06-02).
- [2026-06-02] [during tenant_id threading (usage Phase B)] — `voice/services/dictation_service.py` has ~22 pre-existing ruff errors: TC001/TC002 (move `AsyncSession`, `BedrockService`, `RedisService`, `TranscribeService`, `VoiceRepository` imports into a TYPE_CHECKING block) + UP017 (`datetime.UTC`). NOT introduced by the 2-line tenant_id passthrough. CAUTION: the TC001/TC002 fixes are NOT safe to blind-`--fix` — several of those types are used at runtime (constructor params), so moving them under TYPE_CHECKING would NameError. — **Next action:** hand-audit which imports are runtime vs type-only; only the genuinely type-only ones move. Apply UP017 (`datetime.UTC`) freely. Separate cleanup pass, needs judgment.
- [2026-06-04] [during onboarding prompts/steps flow-grouping refactor — blast-radius verification] — **PRE-EXISTING (NOT caused by the refactor):** client voice stepIds `ndis_plan` (step3) and `medical` (step5) in `lib/core/voice_schemas/{step3_ndis_plan,step5_medical}_schema.dart` do NOT match their backend prompt files `ndis_plan_details.md` / `medical_information.md`. `_step_rules_section` does `rglob("{step_id}.md")` → seeks `ndis_plan.md`/`medical.md`, which never existed (flat or nested) → returns `""` → steps 3 & 5 run with NO step-specific voice rules (base prompt only; graceful, no crash). Behaviour identical before & after the folder move (basenames unchanged). The authored 152-line `ndis_plan_details.md` + 95-line `medical_information.md` therefore never load. Other 4 client steps DO match (personal_information, lifestyle_requirements, documents, consent). — **Next action:** align the contract — either rename backend files `ndis_plan_details.md`→`ndis_plan.md` and `medical_information.md`→`medical.md` (backend-only; activates the dormant prompts) OR change Flutter stepIds. Behaviour-changing (turns on real prompt content for steps 3 & 5) → needs product/QA sign-off, not a silent fix.

## Triaged (moved to TASKS.md or ARCHIVE.md)

*(empty — triage moves entries to one of those files with the date)*
