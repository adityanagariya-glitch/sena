Audit every file in this project and identify all files where switching to or adding XML structure would significantly improve Claude's ability to parse, follow, or act on the content.

For each file found, explain WHY XML would help then apply the change directly without showing before/after previews.

Priority targets to check:
- All .md files in .claude/agents/ (system prompts)
- CLAUDE.md (main project instructions)
- Any .md files containing rules, workflows, constraints, or instructions
- Any prompt files, template files, or config files with structured content
- README files that contain instructions Claude needs to follow

CRITICAL DATA RETENTION RULES — NON-NEGOTIABLE:
- Every single word, character, number, and symbol from the original file MUST be preserved exactly as-is
- Do NOT fix grammar, spelling, punctuation, or phrasing — ever
- Do NOT rephrase, reword, simplify, or "improve" any sentence
- Do NOT add, remove, or change any word even if it looks like a typo
- Do NOT change capitalization, spacing, or line breaks within content
- Your ONLY allowed action is wrapping existing content in XML tags
- If you are unsure whether wrapping a section would require changing even one character, skip that file entirely
- Treat every character as production-critical — one wrong change can break the entire system

Other rules:
- Do NOT change code files (.ts, .js, .py, etc.)
- Do NOT add XML just for the sake of it — only where it genuinely improves structure
- Preserve all YAML frontmatter exactly as-is
- Keep changes minimal and surgical — format only, never meaning

After all changes, output a summary table:
| File | What changed | Why it helps |